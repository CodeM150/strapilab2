'use strict';

/**
 * module-temperature service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::module-temperature.module-temperature');
